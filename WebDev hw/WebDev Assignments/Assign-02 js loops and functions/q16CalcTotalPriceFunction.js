let priceOfItem = 100;
let quantityOfItem = 3;

function calculateTotal(price, quantity) {
    let totalPrice = price * quantity;
    console.log(`Total Price: ${totalPrice}`);
}

calculateTotal(priceOfItem, quantityOfItem);